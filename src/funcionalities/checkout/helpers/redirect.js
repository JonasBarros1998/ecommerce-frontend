/**
 * Função para renderizar o usuario para uma outra pagina
 * 
 * @param url Uma string com o caminho da url completa
 * para renderizar a nova pagina
 */
export const redirectUser = (url) =>{
    window.location.href = url
}