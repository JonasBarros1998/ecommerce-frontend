import { indexedDatabase, updateObjectStore } from '../../../indexed-db/core/database'

const closeCart = (products) => ({type: "CART_COMFIRMATION", products}) 

export const cart = (products) => {

    return (dispatch) => {
        console.log()
    }
}